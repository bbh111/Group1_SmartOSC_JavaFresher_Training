1. Write a generic method to count the number of elements in a collection that have odd
integers, prime numbers
2. Write a generic method to exchange the positions of two different elements in an array
3. Write a generic method to find the maximal element in the range [begin, end) of a list.
4. Modify the following cat method so that it will compile:
public static void cat(File file) {
    RandomAccessFile input = null;
    String line = null;

    try {
        input = new RandomAccessFile(file, "r");
        while ((line = input.readLine()) != null) {
            System.out.println(line);
        }
        return;
    } finally {
        if (input != null) {
            input.close();
        }
    }
}